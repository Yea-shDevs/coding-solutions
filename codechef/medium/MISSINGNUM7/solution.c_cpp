#include <bits/stdc++.h>
using namespace std;

int main() {
    int N;
    cin >> N;

    int cost = (N / 2) * 30;

    if (N % 2 != 0) {
        cost += 20;
    }

    cout << cost << endl;

    return 0;
}