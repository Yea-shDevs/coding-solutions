#include <bits/stdc++.h>

using namespace std;

int main() {
    string str,s;
    cin >> str;
    int count = 0;
    for (int i = 0; i < (str.size()) / 2; i++) {
        count++;
        s = str[i] + s;
    }
    if(count==4 && s==(str[2]+str[3]))
    cout<<"YES";
}