#include <bits/stdc++.h>
using namespace std;

int main() {
    string s1, s;
    cin >> s1;

    s = s1.substr(0, 2);

    if (s == s1.substr(2, 2))
        cout << "YES";
    else
        cout << "NO";

    return 0;
}