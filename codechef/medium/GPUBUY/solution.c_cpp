#include <bits/stdc++.h>
using namespace std;

int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        int X, Y, Z;
        cin >> X >> Y >> Z;
        int i = 1, flag = 0;
        while (flag!=1)
        {
            if(i*Y==i*Z)
            {
            cout<<-1<<endl;
            break;
            }
            if ((X + i*Y) <= i*Z)
            {
                cout << i<<endl;
                flag = 1;
                break;
            }
            i++;
        }
    }

}